namespace arreglos
{
    internal class Program
    {
        static void Main(string[] args)
        {
            arreglitos arreglitos = new arreglitos();
            //int[] arreglo = new int[10];

            // Inicializar el arreglo
            /*arreglitos.InicializarArreglo(arreglo);

            // Mostrar el arreglo


            // Calcular y mostrar la media
            double media = arreglitos.CalcularMedia(arreglo);
            Console.WriteLine("La media de los valores en el arreglo es: " + media);*/

            /*int tamaño;
            int numero;

            Console.Write("Ingrese el tamaño del arreglo: ");
            int.TryParse(Console.ReadLine(), out tamaño);

            Console.Write("Ingrese el número para generar múltiplos: ");
            int.TryParse(Console.ReadLine(), out numero);

            if (tamaño <= 0 || numero <= 0)
            {
                Console.WriteLine("Entrada no válida. Tanto el tamaño como el número deben ser mayores que cero.");
                return;
            }

            int[] arreglo = arreglitos.GenerarMultiplos(tamaño, numero);
            arreglitos.MostrarArreglos(arreglo);
            arreglitos.ProcesarArreglos(); */
        }
    }


